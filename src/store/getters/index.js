export default {
  allEvents: state => {
    // We can modify the order of the events here if required
    return state.allEvents
  },
  allPosts: state => {
    // We can modify the order of the events here if required
    return state.allPosts
  }
}
